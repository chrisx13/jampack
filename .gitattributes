# Normalisation des fins de ligne : les scripts exécutés en conteneur Linux
# doivent rester en LF même sur un checkout Windows (autocrlf), sinon le
# shebang casse (« /usr/bin/env: bash\r: No such file or directory »).
* text=auto
*.sh   text eol=lf
*.sql  text eol=lf
entrypoint.sh text eol=lf
